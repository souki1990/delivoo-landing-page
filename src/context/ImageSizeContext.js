import { createContext } from "react";

const ImageSizeContext = createContext({
    xsWidth: "",
    smallWidth: "",
    mediumWidth: "",
    largeWidth: "",
    xsHeight: ""
});
export default ImageSizeContext;

